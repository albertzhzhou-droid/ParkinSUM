# Copy-Ready Issue Drafts

`gh auth status` was not authenticated when these drafts were prepared, so the
issues were not created on GitHub. Copy each draft into GitHub when ready.

Use the labels suggested in `docs/contribution-backlog.md`. Do not add personal
health information, real medication schedules, credentials, tokens, private
records, raw operator logs, or unsupported medical claims.
